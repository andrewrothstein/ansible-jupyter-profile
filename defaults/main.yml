---
# jupyter_profile_name : jupyter
# jupyter_config_dir : '~/.jupyter'

# jupyter_config_options: []
# Config options listed with key(option name) and value
# eg. - { key : NotebookApp.base_url, value: '/' }
